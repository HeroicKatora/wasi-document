The `stack` scheme interpreter, prepared to run on web/wasi. Prepare by:

```bash
cargo install --root . stak --target wasm32-wasip1
```

This will leave a file `bin/stak.wasm` beneath this directory.
